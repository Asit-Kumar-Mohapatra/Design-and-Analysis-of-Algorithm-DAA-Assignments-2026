SELECTION-SORT(A)
  n = A.length
  for i = 1 to n - 1
      minIndex = i
      for j = i + 1 to n
          if A[j] < A[minIndex]
              minIndex = j
      exchange A[i] with A[minIndex]